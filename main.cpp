#include "Profile.h"
#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    Profile pessoa;
    int run = 1;
    int choice = -1;

    /*pessoa.createprofile();
    while(run == 1)
    {
        cout<<"Selecione a opcao que deseja realizar\n";
        cout<<"[1]Para criar um novo perfil\n"<< "[2]Para realizar Login\n" << "[3]Para mostrar as informacoes de perfil\n"<<"[4]Para sair\n";
        cout<<"--> ";
        cin>>choice;

        switch(choice)
        {
            case 1:
                pessoa.createprofile();
                break;
            case 2:
                pessoa.login();
                break;
            case 3:
                pessoa.showprofile();
                break;
            case 4:
                run = 0;
                break;

        }
    }
    */

    Car carro;
    carro.newcar();

}
